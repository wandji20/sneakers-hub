<div class="container p-3">
  <div>
    <h4 class="text-center" >Sign Up</h4>
  </div>

  <%= render partial: 'form', locals: { user: @user} %>

  <p class="mt-1">
    <span>Already have an Account? </span>
    <span><%= link_to 'Login!', login_path, class: 'text-decoration-none' %></span>
  </p>
</div>
