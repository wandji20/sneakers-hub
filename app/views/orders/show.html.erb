<section class="container">
  <p>
   Use <code>4242 4242 4242 4242</code> for test payment
  </p>
  <div class="d-flex flex-column flex-sm-row mt-4 justify-content-center" id=<%= dom_id(@order)%> >
    <%= render 'order_details', order: @order %>
  </div>
</section>